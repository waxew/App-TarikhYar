// [AS-TEAM-COMMENTED-V1] توضیحات خودکار آموزشی این فایل فعال شده است.
// این فایل تنظیمات Build ماژول Android و وابستگی‌های تاریخ‌یار را مشخص می‌کند.
// راهنما: این بلوک Pluginهای لازم برای ساخت ماژول را فعال می‌کند.
plugins {
    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    id("com.android.application")
    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    id("org.jetbrains.kotlin.android")
    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    id("org.jetbrains.kotlin.plugin.compose")
// راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
}

// راهنما: این بلوک تنظیمات Android مانند SDK، نسخه و Build Typeها را تعریف می‌کند.
android {
    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    namespace = "ir.tarikhyar.app"
    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    compileSdk = 36

    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    defaultConfig {
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        applicationId = "ir.tarikhyar.app"
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        minSdk = 26
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        targetSdk = 36
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        versionCode = 20
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        versionName = "2.0.0"
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
    }

    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    buildTypes {
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        release {
            // نسخه پابلیش صراحتاً Debuggable نیست تا خروجی نهایی با نسخه توسعه اشتباه نشود.
            // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
            isDebuggable = false
            // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
            isMinifyEnabled = false
            // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
            proguardFiles(
                // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
                getDefaultProguardFile("proguard-android-optimize.txt"),
                // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
                "proguard-rules.pro",
            // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
            )
        // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
        }
    // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
    }

    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    compileOptions {
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        sourceCompatibility = JavaVersion.VERSION_17
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        targetCompatibility = JavaVersion.VERSION_17
    // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
    }

    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    kotlinOptions {
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        jvmTarget = "17"
    // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
    }

    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    buildFeatures {
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        compose = true
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        buildConfig = true
    // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
    }

    // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
    packaging {
        // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
        resources {
            // راهنما: این دستور بخشی از منطق، داده یا چیدمان همین بخش را پیاده‌سازی می‌کند.
            excludes += "/META-INF/{AL2.0,LGPL2.1}"
        // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
        }
    // راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
    }
// راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
}

// راهنما: این بلوک کتابخانه‌های مورد نیاز پروژه را به سیستم Build معرفی می‌کند.
dependencies {
    // BOM نسخه کتابخانه‌های Compose را با هم هماهنگ نگه می‌دارد.
    // راهنما: این val یک مقدار فقط‌خواندنی را برای استفاده در منطق جاری نگهداری می‌کند.
    val composeBom = platform("androidx.compose:compose-bom:2026.04.01")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation(composeBom)
    // راهنما: این بلوک تنظیمات Android مانند SDK، نسخه و Build Typeها را تعریف می‌کند.
    androidTestImplementation(composeBom)

    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.activity:activity-compose:1.13.0")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.10.0")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.compose.ui:ui")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.compose.ui:ui-tooling-preview")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.compose.foundation:foundation")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.compose.animation:animation")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.compose.material3:material3")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    implementation("androidx.compose.material:material-icons-extended")

    // ابزارهای Debug فقط در Build توسعه وارد APK می‌شوند و در نسخه Release نهایی حضور ندارند.
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    debugImplementation("androidx.compose.ui:ui-tooling")
    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    debugImplementation("androidx.compose.ui:ui-test-manifest")

    // راهنما: این خط یک وابستگی را برای نوع Build مربوطه به پروژه اضافه می‌کند.
    testImplementation("junit:junit:4.13.2")
// راهنما: این خط محدوده بلوک یا فراخوانی فعلی را باز یا بسته می‌کند.
}
