# CI temporarily paused while TarikhYar 3.0 source is staged.
name: Validate TarikhYar Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TarikhYar 3.0 source staging in progress"
